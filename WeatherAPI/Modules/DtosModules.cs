using Autofac;

namespace WeatherAPI.Modules
{
    public class DtosModules : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
